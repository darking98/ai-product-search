services:
  db:
    image: pgvector/pgvector:pg15
    container_name: ai-products-search-postgresql
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
